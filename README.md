# 📦 Inventory Management System (K3s Edition)

Современная система учета ТМЦ (Товарно-Материальных Ценностей), построенная на микросервисной архитектуре и развернутая в локальном Kubernetes кластере (k3s).

## 🚀 Стек технологий

**Frontend:** React 18+, Tailwind CSS (стилизация), Lucide Icons (иконки), Sonner (уведомления).

**Backend:** Django 5+, Django REST Framework (API), JWT Auth (авторизация).

**Database:** PostgreSQL.

**Infrastructure:** Kubernetes (k3s), Docker, Nginx Ingress Controller.

## 🛠 Архитектура проекта

Проект разделен на три основных слоя:

- **Frontend:** Single Page Application, упакованное в Nginx.
- **Backend:** RESTful API на Python/Django, обрабатывающее бизнес-логику и историю операций.
- **DevOps:** Набор манифестов для деплоя в k3s, включая Ingress для маршрутизации трафика /api.

## ✨ Реализованные фичи

- ✅ **Авторизация:** Защищенный вход через JWT.
- ✅ **Учет ТМЦ:** Полный список инструментов с фильтрацией и поиском.
- ✅ **Выдача в работу:** Привязка ТМЦ к конкретным бригадам.
- ✅ **Боковая панель деталей:** Плавный CSS-интерфейс для просмотра подробной информации без перезагрузки.
- ✅ **История операций:** Автоматическое логирование каждого изменения статуса или перемещения.
- ✅ **Темная тема:** Полная поддержка Dark Mode через Tailwind.

## 📦 Как запустить (Local Dev)

### Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

## ☸️ Деплой в Kubernetes (k3s)

1. Соберите образы:
```bash
docker build -t your-reg/tmc-frontend ./frontend
docker build -t your-reg/tmc-backend ./backend
```

2. Примените манифесты:
```bash
kubectl apply -f k8s/
```

