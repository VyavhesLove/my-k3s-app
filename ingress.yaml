apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-k3s-ingress
spec:
  rules:
  - host: k8s.local
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: django-backend-service
            port:
              number: 8000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: react-frontend-service
            port:
              number: 80

