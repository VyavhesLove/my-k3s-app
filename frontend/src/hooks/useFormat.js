import { formatDate, getDeviceIcon } from '@/utils';

/**
 * Хук для форматирования данных
 * @returns {Object} Функции форматирования
 */
export const useFormat = () => ({
  formatDate,
  getDeviceIcon
});

